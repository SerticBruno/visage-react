import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Profil klijenta · Admin' };

export default function KlijentLayout({ children }: { children: React.ReactNode }) {
  return children;
}
