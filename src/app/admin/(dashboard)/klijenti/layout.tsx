import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Klijenti · Admin' };

export default function KlijentiLayout({ children }: { children: React.ReactNode }) {
  return children;
}
