import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Postavke · Admin' };

export default function PostavkeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
